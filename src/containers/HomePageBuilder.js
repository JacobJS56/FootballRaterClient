import Home from '../components/Home/Home';

const HomePageBuilder = (props) => (
    <div>
        <Home {...props} />
        
        <p> Hi its Ethan</p>
    </div>
);

export default HomePageBuilder;